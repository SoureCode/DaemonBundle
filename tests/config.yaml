framework:
  http_method_override: false
monolog:
  handlers:
    console:
      type: console
      level: debug
    file:
      type: stream
      path: "%kernel.project_dir%/var/logs/debug.log"
      level: debug